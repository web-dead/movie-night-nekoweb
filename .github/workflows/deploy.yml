on: [workflow_dispatch]
#  schedule:
#    - cron: '0 0 * * 1'
  
jobs:
  job1:
    runs-on: ubuntu-latest
    name: setup
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - id: create_package_json
        run: npm init -y
      - id: install_actionsCore
        run: npm install @actions/core
      - id: install_actionsGithub
        run: npm install @actions/github

  job2:
    runs-on: ubuntu-latest
    name: setup
    outputs:
        pointerResult: ${{ steps.run_namePointerChanger.outputs.test }}
    steps:
      - id: run_namePointerChanger
        run: node ./script.js #runs namePointerChanger
      
  job3:
    runs-on: ubuntu-latest
    needs: job2
    steps:
      - env:
          OUTPUT1: ${{needs.job2.outputs.pointerResult}}
        run: echo "$OUTPUT1"
        
   # job3:
     # name: deploy2nekoweb
     # uses: indiefellas/deploy2nekoweb@main
    #  with:
       # nekoweb-api-key: ${{ secrets.NEKOWEB_API_KEY }}
      #  nekoweb-domain: 'web-dead.nekoweb.org'
     #   nekoweb-username: 'web-dead'
      #  directory: 'public'
